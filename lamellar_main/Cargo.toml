[package]
name = "lamellar_main"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true
[dependencies]
syn = {version = "2.0", features = ["full"]}
quote ={version = "1.0"}

[features]
use-srun =[]
use-prterun = []