[package]
name = "lamellar_main"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true
[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = { version = "1.0" }
proc-macro2 = { version = "1.0.101" }

[features]
use-srun = []
use-prterun = []
enable-prof = []
with-salloc = []
